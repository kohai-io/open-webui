# Open WebUI v0.10.2 baseline comparison

Recorded on 2026-07-11 against the official `https://github.com/open-webui/open-webui.git` remote.

## Release identity

| Item | Value |
| --- | --- |
| Release ref | `v0.10.2` |
| Object type | Lightweight tag pointing directly to a commit |
| Commit | `ecd48e2f718220a6400ecf49eafd4867a38feb10` |
| Commit subject | `0.10.2 (#26642)` |
| Commit date | 2026-07-01 03:40:54 -0500 |
| Declared package version | `0.10.2` |
| Local baseline branch | `rebaseline/upstream-v0.10.2` |

The local baseline branch points exactly at the release commit, so its initial source tree contains no fork changes. The current fork and release share ancestor `e0d5de16978786b8a7538adf1efcde5258f38faf`; their comparison is 225 current-fork commits versus 3,147 v0.10.2-side commits.

The commit contains a GitHub GPG signature made with key ID `B5690EEEBB952194`. Local cryptographic verification could not establish trust because the public key is not installed. The commit identity is therefore recorded from the fetched official remote, but independent signature verification remains open.

## Feature path comparison

### Fork products absent from v0.10.2

No corresponding route/domain paths were found at the release commit for:

- `/welcome` and the custom Welcome components
- `/workspace/agents`
- `/workspace/flows`, the Flow router/models, or Flow execution history
- `/workspace/media`
- `/workspace/editor` and timeline components
- the fork's `scheduled_prompt` router/model
- Pi Gateway components

This supports extracting Welcome/Agents presentation, Flows, Media, and timeline work; using upstream Automations instead of porting the scheduled-prompt implementation; and dropping Pi Gateway from the maintained OWUI fork.

### Relevant upstream capabilities present

| Capability | v0.10.2 evidence | Initial conclusion |
| --- | --- | --- |
| Automations | `models/automations.py`, `routers/automations.py`, `utils/automations.py`, `/automations` UI, run history and built-in chat tools | Use upstream exclusively; remove the fork scheduled-prompt implementation and start with no scheduled records |
| Calendar | Calendar model/router/UI and migration `56359461a091` | Use upstream alongside Automations |
| Skills | Skill model/router/API/workspace UI and migration `a1b2c3d4e5f6` | Use upstream; compare fork activation semantics |
| Admin analytics | Analytics router/API and admin dashboard components | Use upstream by default; separately assess whether LiteLLM spend is still required |
| Google Drive | Picker utility, chat upload integration, configuration and backend requirements | Use upstream; compare OAuth/token/sync behaviour before discarding fork code |
| Knowledge | Knowledge router/model/API and expanded directory/file migrations | Use upstream with a fresh empty knowledge store |
| MCP | MCP client and OAuth configuration including token endpoint authentication methods | Use upstream infrastructure; the two fork fixes still need focused code/tests comparison |

## Initial configuration delta

Static extraction of direct `os.getenv(...)` and `os.environ.get(...)` names found 534 names in the current fork and 811 in v0.10.2. This is a source-level lead, not a deployment environment inventory; persistent configuration and indirect reads require separate comparison.

### Current-fork-only names requiring classification

| Variable | Initial classification |
| --- | --- |
| `ENABLE_LITELLM_SPEND` | Custom; decide whether Studio reporting retains it |
| `LITELLM_BASE_URL` | Custom; Studio-owned only if spend reporting is retained |
| `LITELLM_MASTER_KEY` | Custom secret; never browser-visible or copied into planning documents |
| `GOOGLE_DRIVE_CLIENT_SECRET` | Changed integration surface; compare v0.10.2 credential model and require re-authorisation if necessary |
| `ENABLE_API_KEY` | Likely replaced by `ENABLE_API_KEYS`; verify semantics before rename |
| `PORT` | Runtime/container setting; verify deployment-level handling rather than assuming removal |
| `DOCLING_DO_OCR`, `DOCLING_DO_PICTURE_DESCRIPTION`, `DOCLING_FORCE_OCR`, `DOCLING_OCR_ENGINE`, `DOCLING_OCR_LANG`, `DOCLING_PDF_BACKEND`, `DOCLING_PICTURE_DESCRIPTION_API`, `DOCLING_PICTURE_DESCRIPTION_LOCAL`, `DOCLING_PICTURE_DESCRIPTION_MODE`, `DOCLING_PIPELINE`, `DOCLING_TABLE_MODE` | Upstream document-processing configuration changed; map to v0.10.2 settings during staging configuration work |

### Important v0.10.2 additions for this rebaseline

- `ENABLE_AUTOMATIONS`, `AUTOMATION_MAX_COUNT`, `AUTOMATION_MIN_INTERVAL`, `AUTOMATION_POLL_INTERVAL`, and `AUTOMATION_AUTH_TOKEN_EXPIRES_IN`
- `ENABLE_CALENDAR` and calendar permission/alert settings
- `ENABLE_ADMIN_ANALYTICS`
- Skills access/share/import/export permissions
- expanded MCP timeout settings
- OAuth token exchange, session encryption, logout, update, and client behaviour settings

The complete legacy environment must be captured from the deployment host by variable name only, then classified against v0.10.2. Static source comparison cannot complete that task.

## Clean frontend build rehearsal

The untouched `rebaseline/upstream-v0.10.2` branch was checked out at `C:\tmp\owui-v0102-baseline` and tested with portable Node.js `v22.17.0` and npm `10.9.2`, which satisfy the release's declared engine range.

| Check | Result |
| --- | --- |
| `npm ci --ignore-scripts` | Passed: 1,120 packages installed from `package-lock.json` |
| `npm run build` | Passed in 91.4 seconds, including Pyodide asset preparation and Vite production build |
| Generated `build` tree | 5,602 files, 260,156,677 bytes in this Windows rehearsal |
| `npm run check` | Failed on existing upstream Svelte/JavaScript diagnostics, including implicit `any` errors in `RichTextInput` helpers and store typing errors in `CodeBlock.svelte` |
| Dependency audit emitted by install | 19 advisories: 8 low, 8 moderate, 2 high, and 1 critical |

No source files were changed in the baseline worktree. Build warnings included accessibility warnings, large output chunks, browser externalisation warnings for Node modules imported by Pyodide, and a Svelte package export warning. The dependency advisories and type-check failure must be assessed against upstream status; the lockfile was not automatically modified.

This proves that the unmodified frontend production bundle can be generated. Container build and browser smoke tests remain open; the following rehearsal separately covers direct backend startup and initial application health.

## Clean backend startup rehearsal

The untouched baseline was started on 2026-07-11 with Python 3.11.15, an isolated virtual environment, and new storage at `C:\tmp\owui-v0102-data`. Offline mode was enabled and a disposable test secret was supplied through the process environment. No legacy database, uploads, vector store, configuration, or credentials were used.

| Check | Result |
| --- | --- |
| Dependency installation | Passed from `backend/requirements.txt` into a disposable virtual environment |
| Empty SQLite initialisation | Passed; Alembic reached `42e2978c7933` |
| Storage initialisation | Created new database, uploads, cache, and vector database paths |
| `GET /health` | `200 {"status":true}` |
| Public configuration | Reported version `0.10.2` |
| First signup | Passed; the first fresh user received the administrator role and an authentication token |
| Authenticated session | Passed; session identity and administrator role matched the new user |
| Automations | Authenticated list returned `200` with no items |
| Skills | Authenticated list returned `200` with no items |
| Calendar | Authenticated list returned the expected fresh Personal and Scheduled Tasks calendars |
| Schema boundary | Present: `automation`, `automation_run`, `calendar`, `calendar_event`, `skill`; absent: `flow`, `flow_execution`, `scheduled_prompt` |

Startup warnings recorded for later configuration work were wildcard CORS, missing `USER_AGENT`, unavailable FFmpeg/avconv, an Authlib deprecation, and one unexpected embedding-model key during cached model loading. The backend was stopped after the checks. This rehearsal did not test model providers, chat generation, uploads, knowledge retrieval, OAuth, MCP, Google Drive, multi-user permissions, containers, or browser workflows.

## Open verification work

1. Install/trust the GitHub signing key or verify the commit through another trusted channel.
2. Run a focused semantic comparison and regression tests for commits `a608f839c` and `f71acf28e`.
3. Verify a clean database creates only upstream Automation/Calendar objects and no fork `scheduled_prompt` objects.
4. Compare Google Drive scopes, token storage, callback, picker, sync, and revoke behaviour.
5. Extend the fresh-data rehearsal to model providers, chat, uploads, knowledge, OAuth, MCP, Google Drive, and multi-user permissions.
6. Run browser smoke tests against a container or production-like deployment.
7. Triage the upstream type-check failures and dependency advisories without silently changing the baseline.
8. Capture and classify actual deployment variable names without secret values.
